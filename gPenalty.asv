function g = gPenalty(Point,gamma)

%求Penalty检验函数的一阶导
[n,t] = size(x);
if n==0 && t==0
    error('error input Point');
end
g = zeros(n,t);
gfunc = @(x,i) 2*gamma(x(i,1)+2*(
i=0;
while i<n
    g(i,1) = 
end
g

